import { FeatureFilterPipe } from './feature-filter.pipe';

describe('FeatureFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new FeatureFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
